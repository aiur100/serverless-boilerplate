{
  "name": "work-load",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "local-startup": "docker-compose up -d && aws dynamodb create-table --table-name users-table --attribute-definitions AttributeName=userId,AttributeType=S --key-schema AttributeName=userId,KeyType=HASH --billing-mode PAY_PER_REQUEST --endpoint-url http://localhost:8000 > /dev/null 2>&1",
    "dev": "npx nodemon ./api/handler.js",
    "test": "mocha"
  },
  "dependencies": {
    "express": "^4.17.1",
    "lambda-log": "^3.1.0",
    "node-fetch": "^3.2.0",
    "serverless-http": "^2.7.0"
  },
  "devDependencies": {
    "aws-sdk": "^2.1075.0",
    "dotenv": "^16.0.0",
    "mocha": "^9.2.0",
    "nodemon": "^2.0.15"
  }
}
